<Page
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TuristAppV4._1.View"
    xmlns:common="using:TuristAppV4._1.Common"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ViewModel="using:TuristAppV4._1.ViewModel"
    x:Name="pageRoot"
    x:Class="TuristAppV4._1.View.TilfoejRestaurant"
    mc:Ignorable="d">

    <Page.Resources>
        <!-- TODO: Delete this line if the key AppName is declared in App.xaml -->
        <x:String x:Key="AppName">Tilføj ny restaurant</x:String>
    </Page.Resources>

    <Page.DataContext>
        <ViewModel:MainViewModel/>
    </Page.DataContext>

    <!--
        This grid acts as a root panel for the page that defines two rows:
        * Row 0 contains the back button and page title
        * Row 1 contains the rest of the page layout
    -->
    <Grid>
        <Grid.Background>
            <ImageBrush Stretch="Fill" ImageSource="ms-appx:///Assets/bg.png"/>
        </Grid.Background>
        <Grid.ChildrenTransitions>
            <TransitionCollection>
                <EntranceThemeTransition/>
            </TransitionCollection>
        </Grid.ChildrenTransitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="140"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Back button and page title -->
        <Grid>
            <Grid.Background>
                <ImageBrush Stretch="Fill" ImageSource="ms-appx:///Assets/bg.png"/>
            </Grid.Background>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button x:Name="backButton" Margin="39,59,39,0" Command="{Binding NavigationHelper.GoBackCommand, ElementName=pageRoot}"
				Style="{StaticResource NavigationBackButtonNormalStyle}"
				VerticalAlignment="Top"
				AutomationProperties.Name="Back"
				AutomationProperties.AutomationId="BackButton"
				AutomationProperties.ItemType="Navigation Button"/>
            <TextBlock x:Name="pageTitle" Text="{StaticResource AppName}" Style="{StaticResource HeaderTextBlockStyle}" Grid.Column="1" 
				IsHitTestVisible="false" TextWrapping="NoWrap" VerticalAlignment="Bottom" Margin="0,0,30,40"/>
        </Grid>
        <Grid Margin="0,5,0,0" Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="50*"/>
                <RowDefinition Height="50*"/>
                <RowDefinition Height="50*"/>
                <RowDefinition Height="50*"/>
                <RowDefinition Height="50*"/>
                <RowDefinition Height="50*"/>
                <RowDefinition Height="50*"/>
                <RowDefinition Height="100*"/>
                <RowDefinition Height="213*"/>
            </Grid.RowDefinitions>
            <TextBox TextWrapping="Wrap" PlaceholderText="Restaurantnavn" Text="{Binding OpretRestaurantHandler.RestaurantNavn, Mode=TwoWay}" Margin="40 0 40 5" Grid.Row="1"/>

            <TextBox TextWrapping="Wrap" PlaceholderText="Beskrivelse" Text="{Binding OpretRestaurantHandler.Beskrivelse, Mode=TwoWay}" Margin="40 0 40 5" Grid.Row="3"/>
            <TextBox TextWrapping="Wrap" PlaceholderText="Telefon" Text="{Binding OpretRestaurantHandler.Telefon, Mode=TwoWay}" Margin="40 0 40 5" Grid.Row="4"/>
            <TextBox TextWrapping="Wrap" PlaceholderText="Hjemmeside" Text="{Binding OpretRestaurantHandler.Hjemmeside, Mode=TwoWay}" Grid.Row="5" Margin="40 0 40 5"/>

            <ComboBox ItemsSource="{Binding KategoriKatalog}" SelectedItem="{Binding OpretRestaurantHandler.SelectedKategori, Mode=TwoWay}" Margin="40 0 40 5" Grid.Row="6"/>

            <Button Content="Tilføj restaurant" Command="{Binding OpretRestaurantCommand}" Grid.Row="7" HorizontalAlignment="Stretch" Margin="40 10 40 5" VerticalAlignment="Stretch"/>
            <ListBox Width="300" ItemsSource="{Binding ObservableCollectionOfRestaurants2}" Grid.Row="8"/>
            <ComboBox x:Name="comboBox1" SelectedItem="{Binding OpretRestaurantHandler.Bedømmelse, Mode=TwoWay}" Margin="40,0,40,5" Grid.Row="2" PlaceholderText="Vælg bedømmelse">
                <ComboBoxItem Name="cbi1">1</ComboBoxItem>
                <ComboBoxItem Name="cbi2">2</ComboBoxItem>
                <ComboBoxItem Name="cbi3">3</ComboBoxItem>
            </ComboBox>
        </Grid>
    </Grid>
</Page>
