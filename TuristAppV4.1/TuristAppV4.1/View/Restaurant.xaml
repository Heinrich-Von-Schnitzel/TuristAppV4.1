<Page
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TuristAppV4._1.View"
    xmlns:common="using:TuristAppV4._1.Common"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModel="using:TuristAppV4._1.ViewModel"
    x:Name="PageRoot"
    x:Class="TuristAppV4._1.View.Restaurant"
    mc:Ignorable="d">

    <Page.Resources>
        <!-- TODO: Delete this line if the key AppName is declared in App.xaml -->
        <x:String x:Key="AppName">My Application</x:String>
    </Page.Resources>

    <Page.DataContext>
        <viewModel:MainViewModel/>
    </Page.DataContext>

    <!--
        This grid acts as a root panel for the page that defines two rows:
        * Row 0 contains the back button and page title
        * Row 1 contains the rest of the page layout
    -->
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.ChildrenTransitions>
            <TransitionCollection>
                <EntranceThemeTransition/>
            </TransitionCollection>
        </Grid.ChildrenTransitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="140"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Back button and page title -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button x:Name="backButton" Margin="39,59,39,0" Command="{Binding NavigationHelper.GoBackCommand, ElementName=PageRoot}"
				Style="{StaticResource NavigationBackButtonNormalStyle}"
				VerticalAlignment="Top"
				AutomationProperties.Name="Back"
				AutomationProperties.AutomationId="BackButton"
				AutomationProperties.ItemType="Navigation Button"/>
            <TextBlock x:Name="pageTitle" Text="{StaticResource AppName}" Style="{StaticResource HeaderTextBlockStyle}" Grid.Column="1" 
				IsHitTestVisible="false" TextWrapping="NoWrap" VerticalAlignment="Bottom" Margin="0,0,30,40"/>
        </Grid>
        <Grid HorizontalAlignment="Left" Height="599" Margin="30,29,0,0" Grid.Row="1" VerticalAlignment="Top" Width="1326">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="215*"/>
                <ColumnDefinition Width="227*"/>
            </Grid.ColumnDefinitions>
            <Image Source="{Binding SelectedRestaurant.Billede}" HorizontalAlignment="Left" Height="488" Margin="298,68,0,0" VerticalAlignment="Top" Width="314" Grid.Column="1"/>
            <TextBlock HorizontalAlignment="Left" Height="83" Margin="137,88,0,0" TextWrapping="Wrap" Text="{Binding SelectedRestaurant.RestaurantNavn}" VerticalAlignment="Top" Width="334" FontSize="72"/>
            <TextBlock HorizontalAlignment="Left" Margin="126,211,0,0" TextWrapping="Wrap" Text="{Binding SelectedRestaurant.Bedømmelse}" VerticalAlignment="Top" Height="62" Width="197" FontSize="36"/>
            <TextBlock HorizontalAlignment="Left" Margin="137,295,0,0" TextWrapping="Wrap" Text="{Binding SelectedRestaurant.Bedømmelse}" VerticalAlignment="Top" FontSize="36"/>
            <TextBlock HorizontalAlignment="Left" Margin="137,374,0,0" TextWrapping="Wrap" Text="{Binding SelectedRestaurant.Telefon}" VerticalAlignment="Top" FontSize="36"/>
            <TextBlock HorizontalAlignment="Left" Height="57" Margin="137,450,0,0" TextWrapping="Wrap" Text="{Binding SelectedRestaurant.Hjemmeside}" VerticalAlignment="Top" Width="260" FontSize="36"/>
        </Grid>
    </Grid>
</Page>